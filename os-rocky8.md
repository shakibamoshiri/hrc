# OS Rocky Linux 8 useful Linux command line

## how to install openconnect client on rocky Linux

```
# 1
sudo dnf install epel-release

# 2 client 
sudo dnf install openconnect

# 3 server (optional)
sudo dnf install ocserv

```
